import { <%= TableName %>Form } from '../_components/form';

export default function New<%= TableName %>Page() {
  return (
    <div className="space-y-4">
      <div className="flex items-center justify-between">
        <h1 className="text-xl font-bold">New <%= TableWord %></h1>
      </div>

      <<%= TableName %>Form />
    </div>
  );
} 