import { NextRequest } from 'next/server';
import { routeException } from '@/utils/exceptions';
import { <%= tableName %>Service } from '@/repositories/<%= tableName %>/service';


type Props = {
  params: Promise<{ id: string }>;
};

export async function GET(_: NextRequest, { params }: Props) {
  return routeException(async () => {
    const { id } = await params;
    return <%= tableName %>Service.get(<%- answers.pkType === 'number' ? 'Number(id)' : 'id' %>);
  });
}

export async function PUT(request: NextRequest, { params }: Props) {
  return routeException(async () => {
    const { id } = await params;
    const data = await request.json();
    return <%= tableName %>Service.update(<%- answers.pkType === 'number' ? 'Number(id)' : 'id' %>, data);
  });
}

export async function DELETE(_: NextRequest, { params }: Props) {
  return routeException(async () => {
    const { id } = await params;
    return <%= tableName %>Service.delete(<%- answers.pkType === 'number' ? 'Number(id)' : 'id' %>);
  });
} 